# Runtime data
data/servers/

# Environment
.env

# Python
__pycache__/
*.pyc
*.pyo

# IDE
.idea/
.vscode/
*.swp

# Claude Code session scaffolding (worktrees, job dirs)
.claude/

# OS
.DS_Store
Thumbs.db

# Node
node_modules/
frontend/dist/
frontend/package-lock.json

# Generated docs screenshots — reproducible from a running stack via
# docs/capture/ (seed_demo.py + capture.mjs). See docs/capture/README.md.
# Kept out of git so clones don't pull ~22MB of binary build artifacts.
/docs/screenshots/
# Copied out of docs/screenshots/ by website/build-docs.mjs at build time.
/website/docs/screenshots/

# Docker
*.log

.wrangler

# Site-specific deploy pipeline — kept locally for the private CI/CD clone,
# never published to the public mirror (see docs: "Extending the Swarm stack").
.github/workflows/roundhouse-ci.yml
docker-stack.lab.yml
docker-stack.override.yml
