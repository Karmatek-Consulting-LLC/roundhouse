api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":80"
    # Exposed on host as port 3080 to avoid conflict with Herd

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    network: mcp-network
