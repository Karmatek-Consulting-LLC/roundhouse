onlyBuiltDependencies[]=esbuild
# Non-interactive installs (Docker has no TTY); avoids ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY
confirm-modules-purge=false
# Docker Desktop (macOS): bind mount + volume mixes break hardlinks/reflinks; copy is reliable.
package-import-method=copy
